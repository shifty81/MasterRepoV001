# AI Integration Directive

Read the full repository, all included archive zips, all active docs, and all code before making changes.
Then normalize the repo to the current direction using these rules:

1. Rewrite as necessary instead of preserving broken paths.
2. Merge duplicate systems into one authoritative version.
3. Archive stale code and stale docs instead of leaving them in active flow.
4. Do not add new major systems outside the current milestone.
5. Update README, roadmap, and reset docs to match validated reality.
6. Treat editor trustworthiness and persistence as the only immediate milestone.
7. Prefer code that compiles cleanly and matches the repo boundary over code that preserves old experiments.

Immediate implementation order:
1. command history completion
2. voxel edit undo/redo
3. inspector write-back
4. save/load validation
5. outliner/world truth cleanup
6. stale viewport/editor path removal
7. output and launch cleanup
8. minimum usability pass
